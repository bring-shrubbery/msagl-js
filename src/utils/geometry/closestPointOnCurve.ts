import {Point} from './point';
import {ICurve} from './icurve';
export class ClosestPointOnCurve {
  static closestPoint(seg: ICurve, targetPoint: Point, hint: number, low: number, high: number): number {
    throw 'not implemented';
    return hint;
  }
}
